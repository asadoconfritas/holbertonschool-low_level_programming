#include <stdio.h>
#include <stdlib.h>

/**
 * main - add argv values
 * @argc: size of argv
 * @argv: array
 * @i: add argv
 * Return: end program
 **/

int main(int argc, char **argv, int i)

{
	i = 0;

	while(argc--)
	if(
}
