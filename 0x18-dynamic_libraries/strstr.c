#include "main.h"

/**
 * _strstr - work in progress
 * @haystack: char
 * @needle: char
 * Return: end program
 **/

char *_strstr(char *haystack, char *needle)

{
	if (*needle == 0)
	{
		return (haystack);
	}
	return (0);
}
